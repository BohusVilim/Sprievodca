@model IEnumerable<Sprievodca.Models.MainModels.Kraj>

@{
    ViewData["Title"] = "Index";
}

<h1>Kraj</h1>

<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Nazov)
            </th>
            <th></th>
            <th>
                @Html.DisplayNameFor(model => model.Oblast)
            </th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nazov)
            </td>
            <td>
                <a asp-action="Details" asp-route-id="@item.Id">Details</a>
            </td>
                @if (item.Oblast != null && item.Oblast.Count > 0)
                {
                    <td>
                        <select class="form-control" onchange="window.location.href=this.value;">
                            <option value="">-- Vyber oblasť --</option>
                            @{
                                foreach (var oblast in item.Oblast)
                                {
                                    if (oblast.Name != null)
                                    {
                                        <option value="@Url.Action("Details", "Oblast", new { id = oblast.Id })">@oblast.Name</option>
                                    }
                                }
                            }
                        </select>
                        <script>
                            document.getElementById("oblast-select").onchange = function () {
                                var url = this.value;
                                if (url) {
                                    window.location.href = url;
                                }
                                return false;
                            };
                        </script>
                    </td>
                }
        </tr>
}
    </tbody>
</table>

<div class="container">
    <a asp-area="" asp-controller="Home" asp-action="Admin">Back</a>
</div>
